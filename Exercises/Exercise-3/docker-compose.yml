version: "3.9"
services:
  test:
    image: "exercise-3"
    volumes:
      - .:/app
    command: python3 -m pytest
  run:
    image: "exercise-3"
    volumes:
      - .:/app
      - ~/.aws:/root/.aws:ro
    command: python3 main.py